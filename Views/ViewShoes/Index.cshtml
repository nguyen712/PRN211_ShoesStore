@page
@model PRN211_ShoesStore.Views.ViewShoes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Shoes[0].name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shoes[0].price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shoes[0].shoesDetails)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shoes[0].launchDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shoes[0].status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shoes[0].quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Shoes) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.shoesDetails)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.launchDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.quantity)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
